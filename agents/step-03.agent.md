# Step 03

Added the initial usage docs and tests for the first flow.

What was added:
- README quick start and API section
- unit/integration-style tests using the memory adapter

Why:
- so the package is easier to validate, extend, and publish as an npm library
