# Step 06

Renamed the package for npm availability.

What was added:
- package name changed from `otp-manager` to `redis-otp-manager`
- README install and import examples updated

Why:
- so the package can be published under an available npm name without using a scoped package yet
