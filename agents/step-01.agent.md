# Step 01

Initialized the package as a TypeScript npm library.

What was added:
- base folder structure
- `package.json`
- `tsconfig.json`
- `.gitignore`

Why:
- to make the package buildable and ready for the OTP manager implementation
